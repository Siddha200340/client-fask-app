# Client Flask App

This is a simple Flask application provided by the client for containerization and deployment practice.

## Features
- Home endpoint: `/`
- About endpoint: `/about`
- Configurable via environment variables:
  - `FLASK_APP_SECRET_KEY`
  - `FLASK_APP_PORT`

